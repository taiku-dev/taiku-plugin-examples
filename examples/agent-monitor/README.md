# Agent Monitor

Built-in taiku plugin for tracking AI agent activity across shells.

What it demonstrates:

- subscribing to agent and session events
- rendering a richer static sidebar panel
- hotkeys and toolbar actions on a production plugin
