# Event Log

Built-in taiku panel for browsing live session events.

What it demonstrates:

- subscribing to many event types
- rendering a bottom panel instead of a sidebar panel
- filtering and organizing event streams in a static plugin
